# debugging
    # c for continuing debugging
    # help for list of all commands

import math

def hypotenuse(leg1: float, leg2: float):
    return (math.sqrt(leg1**2 + leg2**2))

def main():
    print(hypotenuse(1.0, 1.0))