def no_vowels(myStr : str):
    myStr = myStr.replace("i", "")
    myStr = myStr.replace("a", "")
    myStr = myStr.replace("e", "")
    myStr = myStr.replace("o", "")
    myStr = myStr.replace("u", "")
    return (myStr)